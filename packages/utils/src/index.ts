const year = new Date().getFullYear();

export const sessions = [
  `${year - 1}-${year}`,
  `${year}-${year + 1}`,
  `${year + 1}-${year + 2}`,
];

export const nationalities = ["Bangladeshi", "Other"];
